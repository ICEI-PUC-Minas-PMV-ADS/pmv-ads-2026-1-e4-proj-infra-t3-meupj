info:
  name: Orders Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: '{{baseUrl}}/api/orders/{{orderId}}'
  headers:
    - name: Cookie
      value: '{{sessionCookie}}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

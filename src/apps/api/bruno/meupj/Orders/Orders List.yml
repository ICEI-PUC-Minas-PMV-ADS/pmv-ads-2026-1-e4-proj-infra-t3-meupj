info:
  name: Orders List
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/api/orders?page=1&limit=20&sortBy=createdAt&sortOrder=desc'
  headers:
    - name: Cookie
      value: sessionCookie
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

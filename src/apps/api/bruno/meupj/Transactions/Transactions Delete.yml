info:
  name: Transactions Delete
  type: http
  seq: 7

http:
  method: DELETE
  url: '{{baseUrl}}/api/transactions/{{transactionId}}'
  headers:
    - name: Cookie
      value: '{{sessionCookie}}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

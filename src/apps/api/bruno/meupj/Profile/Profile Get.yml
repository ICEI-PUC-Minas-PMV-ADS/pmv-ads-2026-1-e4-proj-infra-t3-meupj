info:
  name: Profile Get
  type: http
  seq: 1

http:
  method: GET
  url: '{{baseUrl}}/api/profile'
  headers:
    - name: Cookie
      value: '{{sessionCookie}}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Documents Receipt JSON
  type: http
  seq: 5

http:
  method: GET
  url: '{{baseUrl}}/api/documents/receipt/{{transactionId}}'
  headers:
    - name: Cookie
      value: '{{sessionCookie}}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

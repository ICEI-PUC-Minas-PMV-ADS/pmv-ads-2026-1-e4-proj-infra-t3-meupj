info:
  name: Documents Service Order JSON
  type: http
  seq: 3

http:
  method: GET
  url: '{{baseUrl}}/api/documents/service-order/{{orderId}}'
  headers:
    - name: Cookie
      value: '{{sessionCookie}}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

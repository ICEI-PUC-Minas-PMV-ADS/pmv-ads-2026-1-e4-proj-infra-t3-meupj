info:
  name: Documents Budget JSON
  type: http
  seq: 1

http:
  method: GET
  url: '{{baseUrl}}/api/documents/budget/{{orderId}}'
  headers:
    - name: Cookie
      value: '{{sessionCookie}}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

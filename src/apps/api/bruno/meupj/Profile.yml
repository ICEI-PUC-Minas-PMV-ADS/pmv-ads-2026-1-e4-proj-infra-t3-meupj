info:
  name: Profile
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/profile"
  headers:
    - name: Cookie
      value: sessionCookie
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
